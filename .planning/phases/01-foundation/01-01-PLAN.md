---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - biome.jsonc
  - .gitignore
  - .nvmrc
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/postcss.config.mjs
  - apps/web/app/layout.tsx
  - apps/web/app/(dashboard)/page.tsx
  - apps/web/app/globals.css
  - apps/web/vitest.config.ts
  - packages/db/package.json
  - packages/db/tsconfig.json
  - packages/db/vitest.config.ts
  - packages/db/src/index.ts
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/src/index.ts
  - packages/ui/src/styles/globals.css
  - packages/ui/postcss.config.mjs
  - tooling/tsconfig/base.json
  - tooling/eslint-config/package.json
  - tooling/eslint-config/index.js
  - eslint.config.js
  - vitest.shared.ts
autonomous: true
requirements:
  - FOUND-01
user_setup: []

must_haves:
  truths:
    - "A new contributor can run `pnpm install` at the repo root and the workspace resolves cleanly with no missing peers."
    - "`pnpm typecheck` runs across all workspace packages and exits 0 on the bare scaffold."
    - "`pnpm lint` (Biome + ESLint sidecar) runs across all workspace packages and exits 0 on the bare scaffold."
    - "`pnpm dev` starts the Next.js 16 app on port 3000 with the bare placeholder page."
    - "`pnpm test` runs through Turbo and executes the empty Wave 0 stub tests in `apps/web` and `packages/db` without error."
  artifacts:
    - path: "package.json"
      provides: "Workspace root with pnpm@10, packageManager pin, and bootstrap/dev/build/test/lint/typecheck/db:* scripts"
      contains: "\"packageManager\""
    - path: "pnpm-workspace.yaml"
      provides: "Workspace globs for apps/*, packages/*, tooling/*"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Turbo task graph (build, dev, typecheck, lint, test, db:*) per RESEARCH.md `turbo.json` minimum-viable example"
      contains: "\"tasks\""
    - path: "apps/web/package.json"
      provides: "Next.js 16.x + React 19 application package named @indiepilot/web"
      contains: "\"next\""
    - path: "apps/web/next.config.ts"
      provides: "Next config with transpilePackages: ['@indiepilot/ui', '@indiepilot/db'] and output: 'standalone'"
      contains: "transpilePackages"
    - path: "apps/web/app/(dashboard)/page.tsx"
      provides: "Bare placeholder route under (dashboard) group so the Next dev server has a page to render"
    - path: "packages/db/package.json"
      provides: "Empty @indiepilot/db package shell with name + main + types entries (Drizzle install lands in Plan 02)"
      contains: "@indiepilot/db"
    - path: "packages/ui/package.json"
      provides: "@indiepilot/ui package shell with shadcn-CLI-installable structure"
      contains: "@indiepilot/ui"
    - path: "tooling/eslint-config/package.json"
      provides: "Sidecar ESLint config package that hosts custom rules (no-raw-log lands in Plan 03)"
    - path: "biome.jsonc"
      provides: "Root Biome 2.x config — formatter + linter rules for ts/tsx"
      contains: "$schema"
    - path: "eslint.config.js"
      provides: "Root ESLint 9 flat config that loads the @indiepilot/eslint-config sidecar (rules wired in Plan 03)"
    - path: "apps/web/vitest.config.ts"
      provides: "Vitest 3.2 config for the web app"
    - path: "packages/db/vitest.config.ts"
      provides: "Vitest 3.2 config for the db package"
    - path: ".gitignore"
      provides: "Ignores node_modules/, .turbo/, .next/, .env*, **/.vercel/, dist/, build/, coverage/, .DS_Store"
      contains: ".env"
  key_links:
    - from: "apps/web/next.config.ts"
      to: "packages/ui + packages/db"
      via: "transpilePackages array"
      pattern: "transpilePackages.*@indiepilot/ui.*@indiepilot/db"
    - from: "package.json (root)"
      to: "Turbo + per-package scripts"
      via: "scripts.dev / scripts.test / scripts.lint / scripts.typecheck delegate to `turbo run`"
      pattern: "turbo run"
    - from: "eslint.config.js"
      to: "tooling/eslint-config"
      via: "require('./tooling/eslint-config') in flat config"
    - from: "apps/web + packages/db"
      to: "tooling/tsconfig/base.json"
      via: "tsconfig `extends` field"
      pattern: "\"extends\".*tooling/tsconfig"

execution_notes:
  - "Plan 01 touches ~30 files across 3 tasks (workspace root + 3 package shells + Wave 0 test stubs). This is acceptable for greenfield bootstrap because each file is small (mostly config/manifests) and the work is mechanical. CONTEXT-BUDGET MONITORING: executor should track context usage per task — if Task 1 alone consumes >25% context, pause and split rather than push through degraded quality. Target: each task lands in ~15-20% context, total plan in ~50%."
---

<objective>
Stand up the bare pnpm + Turborepo monorepo skeleton for IndiePilot: workspace root, `apps/web` (Next.js 16.2.4), `packages/db` (empty shell), `packages/ui` (empty shell), `tooling/` (shared tsconfig + ESLint sidecar package), Biome 2.x baseline, ESLint 9 flat config sidecar, Vitest 3.2 wired per package with Wave 0 stub test files in place, and a clean `.gitignore`. No Drizzle, no Pino, no Inngest yet — just a runnable scaffold that downstream Wave 2 plans land into.

Purpose: Phase 1's foundation. Plans 02 (DB), 03 (Logger + ESLint rule), 04 (Inngest), 05 (CI), and 06 (Bootstrap) all assume this scaffold exists. Get the workspace boundaries right once so they don't get retrofit later.

Output: A monorepo where `pnpm install && pnpm dev` produces a Next.js 16 page at `http://localhost:3000`, where `pnpm typecheck && pnpm lint && pnpm test` all pass on the empty scaffold, and where Wave 0 stub test files exist (failing-but-skipped or returning trivial green) so Wave 2 plans can land their assertions without scaffolding test infra mid-task.
</objective>

<execution_context>
@/Users/vincentvillalta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vincentvillalta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- This is greenfield — no existing source files to extract from. -->
<!-- Locked architectural shapes from CONTEXT/RESEARCH that downstream plans depend on: -->

Workspace package names (canonical):
- @indiepilot/web        → apps/web
- @indiepilot/db         → packages/db
- @indiepilot/ui         → packages/ui
- @indiepilot/eslint-config → tooling/eslint-config

Locked version pins:
- next: 16.2.4 (Active LTS — see RESEARCH "State of the Art")
- react / react-dom: 19.x (latest)
- typescript: 5.6+
- pnpm: 10.x (pin via `packageManager` field)
- turbo: ^2.x
- @biomejs/biome: 2.3.x
- eslint: ^9.x (sidecar)
- vitest: ^3.2
- @playwright/test: ^1.x
- @types/node: 20.x

Route group placeholders Phase 1 must establish (empty / placeholder pages only):
- app/(dashboard)/* — Phase 2 fills with real dashboard
- app/(marketing)/* — Phase 3 fills (Phase 1 may leave directory empty)
- app/api/inngest/route.ts — Plan 04 creates this; Plan 01 just leaves the api/ directory empty

Tailwind v4 ownership: packages/ui owns tailwind via @import "tailwindcss" + @source directives;
  apps/web/app/globals.css is a one-line @import of the ui package CSS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize pnpm + Turborepo workspace root with Biome + ESLint sidecar + shared tsconfig</name>
  <files>
    package.json,
    pnpm-workspace.yaml,
    turbo.json,
    tsconfig.base.json,
    tooling/tsconfig/base.json,
    tooling/eslint-config/package.json,
    tooling/eslint-config/index.js,
    biome.jsonc,
    eslint.config.js,
    .gitignore,
    .nvmrc,
    vitest.shared.ts
  </files>
  <action>
    Create the workspace root files. Run `pnpm init` to seed `package.json`, then overwrite to match the layout below. Do not run `pnpm install` until all files are written.

    1. Root `package.json`:
       - `"name": "indiepilot"`, `"private": true`, `"version": "0.0.0"`
       - `"packageManager": "pnpm@10.x.x"` (use the exact current pnpm 10 patch — `pnpm --version` after corepack enable)
       - Scripts (mirror RESEARCH "Pattern 5"): `bootstrap`, `env:pull`, `dev`, `build`, `typecheck`, `lint`, `lint:fix`, `test`, `db:generate`, `db:migrate`, `db:seed`, `db:studio`, `db:reset`. The `db:*` scripts delegate via `pnpm --filter @indiepilot/db ...` (db package will fill them in Plan 02 — that's fine, the script can fail gracefully today since db has no scripts yet).
       - `"lint": "biome check . && eslint ."` — both must pass.
       - DevDeps at root: `turbo@^2`, `@biomejs/biome@^2.3`, `eslint@^9`, `vitest@^3.2`, `@playwright/test@^1`, `typescript@^5.6`, `@types/node@^20`.
       - Add `"engines": { "node": ">=20" }`.

    2. `pnpm-workspace.yaml`:
       ```yaml
       packages:
         - "apps/*"
         - "packages/*"
         - "tooling/*"
       ```

    3. `turbo.json` — copy verbatim from RESEARCH "Pattern: turbo.json (minimum viable)" but ensure the `env` array on `build` lists EXACTLY (no duplicates): `DATABASE_URL`, `NEON_BRANCH`, `AXIOM_TOKEN`, `AXIOM_DATASET`, `INNGEST_SIGNING_KEY`, `INNGEST_EVENT_KEY`, `LOG_HASH_SALT`, `LOG_LEVEL`, `VERCEL_ENV`, `NODE_ENV`. Include tasks: `build`, `dev` (cache:false, persistent:true), `typecheck`, `lint`, `test`, `db:generate`, `db:migrate`, `db:seed` — all with appropriate dependsOn / cache flags from RESEARCH.

    4. `tsconfig.base.json` AND `tooling/tsconfig/base.json` — keep one source of truth in `tooling/tsconfig/base.json`; root `tsconfig.base.json` is a thin re-export (`{ "extends": "./tooling/tsconfig/base.json" }`). Base config: `"target": "ES2022"`, `"module": "ESNext"`, `"moduleResolution": "Bundler"`, `"strict": true`, `"noUncheckedIndexedAccess": true`, `"verbatimModuleSyntax": true`, `"skipLibCheck": true`, `"jsx": "preserve"`, `"esModuleInterop": true`, `"resolveJsonModule": true`, `"isolatedModules": true`, `"incremental": true`. Add `"types": ["node"]`. Do NOT include `"paths"` here — apps/packages add those locally.

    5. `tooling/eslint-config/package.json`:
       - `"name": "@indiepilot/eslint-config"`, `"private": true`, `"version": "0.0.0"`
       - `"main": "./index.js"`, `"type": "commonjs"` (custom ESLint rules ship as CJS; flat config consumes via require()).
       - Scripts: `"test": "vitest run"` (Plan 03 wires the actual test).

    6. `tooling/eslint-config/index.js` — minimal stub: `module.exports = [];` (Plan 03 fills with the no-raw-log rule).

    7. `biome.jsonc` — Biome 2.3 config:
       - `"$schema"` pointing to https://biomejs.dev/schemas/2.3.0/schema.json
       - `"organizeImports": { "enabled": true }`
       - `"linter": { "enabled": true, "rules": { "recommended": true } }`
       - `"formatter": { "enabled": true, "indentStyle": "space", "indentWidth": 2, "lineWidth": 100 }`
       - `"javascript": { "formatter": { "quoteStyle": "double", "semicolons": "always", "trailingCommas": "all" } }`
       - `"files": { "ignore": ["**/.next/**", "**/dist/**", "**/.turbo/**", "**/drizzle/**", "**/coverage/**", "**/.vercel/**"] }`

    8. `eslint.config.js` — ESLint 9 flat config (CJS form):
       ```js
       const indiepilot = require("@indiepilot/eslint-config");
       module.exports = [...indiepilot];
       ```
       (Plan 03 wires the actual rules into the sidecar package.)

    9. `.gitignore` — copy verbatim from RESEARCH "Pattern 5: `.gitignore` essentials" then add: `*.tsbuildinfo`, `next-env.d.ts`.

    10. `.nvmrc` — single line: `20`

    11. `vitest.shared.ts` — root shared config:
        ```ts
        import { defineConfig } from "vitest/config";
        export default defineConfig({
          test: {
            reporters: ["default"],
            coverage: { provider: "v8", reporter: ["text", "lcov"] },
          },
        });
        ```
        Each package's vitest.config.ts will `mergeConfig(sharedConfig, ...)` it.

    DO NOT run `pnpm install` yet — Tasks 2 and 3 add subpackage manifests, then a single install at the end.

    Why this layout: matches RESEARCH "Recommended Project Structure" exactly; downstream plans (02-06) reference these exact file paths and script names. Don't deviate from the names — Plan 02 imports `@indiepilot/db`, Plan 03 imports `@indiepilot/eslint-config`, etc.

    **Context-budget watch:** Task 1 is the largest of the three (10+ files of root config). Aim to land it in <25% context. If you find yourself near 30% before all 11 file-writes are complete, stop and signal — the plan can be split rather than degrade.
  </action>
  <verify>
    <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f turbo.json && test -f biome.jsonc && test -f eslint.config.js && test -f .gitignore && test -f tooling/tsconfig/base.json && test -f tooling/eslint-config/index.js && test -f vitest.shared.ts && grep -q '"packageManager"' package.json && grep -q "apps/\*" pnpm-workspace.yaml && grep -q "tooling/\*" pnpm-workspace.yaml && [ "$(grep -c '"AXIOM_DATASET"' turbo.json)" -eq 1 ]</automated>
  </verify>
  <done>Root workspace files exist. `pnpm-workspace.yaml` declares `apps/*`, `packages/*`, `tooling/*`. `package.json` pins `pnpm@10` via `packageManager` field and declares all `bootstrap/dev/build/test/lint/typecheck/db:*` scripts. `turbo.json` declares the task graph from RESEARCH with NO duplicate env entries (AXIOM_DATASET appears exactly once). `biome.jsonc` + `eslint.config.js` exist with the sidecar wiring. `.gitignore` blocks `.env*`, `**/.vercel/`, `node_modules/`, `.next/`, `.turbo/`. `tooling/tsconfig/base.json` is the single TypeScript config source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold apps/web Next.js 16 app + packages/ui + packages/db package shells</name>
  <files>
    apps/web/package.json,
    apps/web/tsconfig.json,
    apps/web/next.config.ts,
    apps/web/postcss.config.mjs,
    apps/web/app/layout.tsx,
    apps/web/app/(dashboard)/page.tsx,
    apps/web/app/globals.css,
    apps/web/vitest.config.ts,
    apps/web/lib/.gitkeep,
    apps/web/inngest/.gitkeep,
    packages/ui/package.json,
    packages/ui/tsconfig.json,
    packages/ui/src/index.ts,
    packages/ui/src/styles/globals.css,
    packages/ui/postcss.config.mjs,
    packages/db/package.json,
    packages/db/tsconfig.json,
    packages/db/vitest.config.ts,
    packages/db/src/index.ts
  </files>
  <action>
    Scaffold the three workspace packages. Hand-write the files (do NOT run `create-next-app` — it pollutes with extra config we don't want, and the Next 16 App Router shape is small enough to write directly).

    **apps/web/package.json:**
    - `"name": "@indiepilot/web"`, `"private": true`, `"version": "0.0.0"`, `"type": "module"`
    - Scripts: `"dev": "next dev --turbo"`, `"build": "next build"`, `"start": "next start"`, `"typecheck": "tsc --noEmit"`, `"lint": "biome check . && eslint ."`, `"test": "vitest run"`
    - Deps: `next@16.2.4`, `react@^19`, `react-dom@^19`, `zod@^3.23`, `@indiepilot/ui@workspace:*`, `@indiepilot/db@workspace:*`
    - DevDeps: `typescript@^5.6`, `@types/node@^20`, `@types/react@^19`, `@types/react-dom@^19`, `tailwindcss@^4.1`, `@tailwindcss/postcss@^4.1`, `@vitejs/plugin-react@^4`, `@indiepilot/eslint-config@workspace:*`

    **apps/web/tsconfig.json:**
    ```json
    {
      "extends": "../../tooling/tsconfig/base.json",
      "compilerOptions": {
        "lib": ["dom", "dom.iterable", "ES2022"],
        "jsx": "preserve",
        "plugins": [{ "name": "next" }],
        "paths": { "@/*": ["./*"] }
      },
      "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
      "exclude": ["node_modules", ".next"]
    }
    ```

    **apps/web/next.config.ts** — copy verbatim from RESEARCH "Pattern 6":
    ```ts
    import type { NextConfig } from "next";
    export default {
      transpilePackages: ["@indiepilot/ui", "@indiepilot/db"],
      output: "standalone",
      outputFileTracingRoot: "../../",
    } satisfies NextConfig;
    ```

    **apps/web/postcss.config.mjs:**
    ```js
    export default { plugins: { "@tailwindcss/postcss": {} } };
    ```

    **apps/web/app/globals.css** — single line:
    ```css
    @import "@indiepilot/ui/styles/globals.css";
    ```

    **apps/web/app/layout.tsx** — minimal RootLayout:
    ```tsx
    import "./globals.css";
    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="en">
          <body>{children}</body>
        </html>
      );
    }
    ```

    **apps/web/app/(dashboard)/page.tsx** — placeholder so dev server has something to render:
    ```tsx
    export default function DashboardPlaceholder() {
      return <main style={{ padding: 24 }}>IndiePilot — Phase 1 scaffold OK</main>;
    }
    ```

    **apps/web/vitest.config.ts:**
    ```ts
    import { mergeConfig, defineConfig } from "vitest/config";
    import sharedConfig from "../../vitest.shared";
    export default mergeConfig(sharedConfig, defineConfig({
      test: {
        environment: "node",
        include: ["**/__tests__/**/*.test.ts", "**/*.test.ts"],
      },
    }));
    ```

    **apps/web/lib/.gitkeep** and **apps/web/inngest/.gitkeep** — empty files so the directories track in git. Plan 03 fills `lib/logger.ts` etc; Plan 04 fills `inngest/`.

    **packages/ui/package.json:**
    - `"name": "@indiepilot/ui"`, `"private": true`, `"type": "module"`
    - `"exports": { ".": "./src/index.ts", "./styles/globals.css": "./src/styles/globals.css" }`
    - `"main": "./src/index.ts"`, `"types": "./src/index.ts"`
    - Scripts: `"typecheck": "tsc --noEmit"`, `"lint": "biome check . && eslint ."`, `"test": "vitest run"`
    - Deps: `react@^19`, `react-dom@^19`, `tailwindcss@^4.1`
    - DevDeps: `typescript@^5.6`, `@types/react@^19`, `@types/react-dom@^19`, `@indiepilot/eslint-config@workspace:*`

    **packages/ui/tsconfig.json:**
    ```json
    {
      "extends": "../../tooling/tsconfig/base.json",
      "compilerOptions": {
        "lib": ["dom", "dom.iterable", "ES2022"],
        "jsx": "preserve",
        "outDir": "./dist"
      },
      "include": ["src/**/*"]
    }
    ```

    **packages/ui/src/index.ts** — empty barrel:
    ```ts
    export {};
    ```

    **packages/ui/src/styles/globals.css** — copy from RESEARCH "Pattern 6":
    ```css
    @import "tailwindcss";
    @source "../../../apps/web";
    @source "../../../packages/ui/src";

    @theme {
      --color-brand-50: oklch(0.97 0.02 80);
      /* Phase 2 BRAND-01 fills in the real palette; Phase 1 ships placeholder */
    }
    ```

    **packages/ui/postcss.config.mjs:**
    ```js
    export default { plugins: { "@tailwindcss/postcss": {} } };
    ```

    **packages/db/package.json:**
    - `"name": "@indiepilot/db"`, `"private": true`, `"type": "module"`
    - `"exports": { ".": "./src/index.ts" }`
    - `"main": "./src/index.ts"`, `"types": "./src/index.ts"`
    - Scripts: `"typecheck": "tsc --noEmit"`, `"lint": "biome check . && eslint ."`, `"test": "vitest run"` (Plan 02 adds db:generate, db:migrate, db:seed, db:studio, db:reset)
    - DevDeps: `typescript@^5.6`, `@indiepilot/eslint-config@workspace:*`

    **packages/db/tsconfig.json:**
    ```json
    {
      "extends": "../../tooling/tsconfig/base.json",
      "compilerOptions": { "outDir": "./dist" },
      "include": ["src/**/*", "tests/**/*", "seed/**/*"]
    }
    ```

    **packages/db/src/index.ts** — empty barrel (Plan 02 fills):
    ```ts
    export {};
    ```

    **packages/db/vitest.config.ts:**
    ```ts
    import { mergeConfig, defineConfig } from "vitest/config";
    import sharedConfig from "../../vitest.shared";
    export default mergeConfig(sharedConfig, defineConfig({
      test: {
        environment: "node",
        include: ["tests/**/*.test.ts"],
      },
    }));
    ```

    DO NOT run `pnpm install` yet — Task 3 also writes Wave 0 stub test files, then a single install runs.

    Why this layout: matches RESEARCH "Recommended Project Structure" — `apps/web/lib/`, `apps/web/inngest/`, `packages/db/src/schema/`, `packages/ui/src/components/` directories exist (via .gitkeep where empty) so downstream plans land into known paths. The Tailwind v4 + shadcn-monorepo CSS ownership in `packages/ui` matches Pitfall 7's mitigation.
  </action>
  <verify>
    <automated>test -f apps/web/package.json && test -f apps/web/next.config.ts && test -f apps/web/app/layout.tsx && test -f apps/web/app/\(dashboard\)/page.tsx && test -f apps/web/vitest.config.ts && test -f packages/ui/package.json && test -f packages/ui/src/styles/globals.css && test -f packages/db/package.json && test -f packages/db/vitest.config.ts && grep -q "transpilePackages" apps/web/next.config.ts && grep -q "@indiepilot/ui" apps/web/package.json && grep -q "@indiepilot/db" apps/web/package.json && grep -q "workspace:\*" apps/web/package.json</automated>
  </verify>
  <done>`apps/web/` is a Next.js 16 application package consuming `@indiepilot/ui` + `@indiepilot/db` via workspace protocol. `packages/ui/` owns Tailwind v4 + the brand placeholder CSS. `packages/db/` is an empty TS package shell that Plan 02 will fill. All three packages extend `tooling/tsconfig/base.json`. Vitest configs exist per package and merge `vitest.shared.ts`. No install has run yet — Task 3 finishes scaffolding then runs the single install.</done>
</task>

<task type="auto">
  <name>Task 3: Write Wave 0 stub test files, install dependencies, and verify scaffold runs end-to-end</name>
  <files>
    apps/web/lib/__tests__/logger.test.ts,
    apps/web/app/api/inngest/__tests__/route.test.ts,
    apps/web/inngest/functions/__tests__/health.test.ts,
    packages/db/tests/schema.test.ts,
    packages/db/tests/types.test.ts,
    packages/db/tests/seed.test.ts,
    tooling/eslint-config/__tests__/no-raw-log.test.js
  </files>
  <action>
    Land the Wave 0 stub test files from `01-VALIDATION.md`. Each stub should be a single passing-but-trivial Vitest test (e.g., `it.todo("...")` or `expect(true).toBe(true)`) so the test suite is GREEN today and Plans 02/03/04 can replace the trivial assertion with the real one without scaffolding test infra mid-task.

    **Pattern for each Wave 0 stub:**
    ```ts
    import { describe, it } from "vitest";
    describe("FOUND-XX: <behavior>", () => {
      it.todo("<assertion that Plan NN will fill in>");
    });
    ```
    Vitest's `it.todo` reports as "todo" (not failure) so `pnpm test` exits 0.

    **Stubs to create:**

    1. `apps/web/lib/__tests__/logger.test.ts` — Plan 03 fills:
       ```ts
       import { describe, it } from "vitest";
       describe("FOUND-04: logger redaction", () => {
         it.todo("redacts `.p8`, `.privateKey`, `.jwt`, `.password`, `*.ascToken` paths to `[redacted]`");
         it.todo("`req.headers.cookie` and `req.headers.authorization` are redacted in nested request shapes");
         it.todo("redaction works for both top-level and `*.<key>` deeply nested shapes per Pino path syntax");
       });
       describe("FOUND-04: hashEmail", () => {
         it.todo("returns the same hash for the same email + salt across calls");
         it.todo("returns different hashes for different salts (per-deploy unlinkability)");
         it.todo("normalizes case + whitespace before hashing");
       });
       describe("FOUND-04: transport fallback", () => {
         it.todo("does not throw when AXIOM_TOKEN is unset (falls back to stdout)");
       });
       ```

    2. `apps/web/app/api/inngest/__tests__/route.test.ts` — Plan 04 fills:
       ```ts
       import { describe, it } from "vitest";
       describe("FOUND-05: Inngest route handler", () => {
         it.todo("GET /api/inngest returns 200 with introspection JSON");
         it.todo("registered functions list contains health-check");
       });
       ```

    3. `apps/web/inngest/functions/__tests__/health.test.ts` — Plan 04 fills:
       ```ts
       import { describe, it } from "vitest";
       describe("FOUND-05: health.check function", () => {
         it.todo("function id is 'health-check'");
         it.todo("function trigger event name is 'health/check.requested'");
         it.todo("handler returns { ok: true, receivedAt: <number> }");
       });
       ```

    4. `packages/db/tests/schema.test.ts` — Plan 02 fills:
       ```ts
       import { describe, it } from "vitest";
       describe("FOUND-02: schema exports", () => {
         it.todo("exports `developers` table with id, email, createdAt, updatedAt, deletedAt");
         it.todo("exports `users` table with id, email, createdAt, updatedAt, deletedAt");
         it.todo("exports `sessions` table (Better-Auth-compatible shape)");
         it.todo("exports `auditLog` table with polymorphic columns + 3 indexes");
         it.todo("`developers.email` and `users.email` have unique indexes");
       });
       ```

    5. `packages/db/tests/types.test.ts` — Plan 02 fills:
       ```ts
       import { describe, it } from "vitest";
       describe("FOUND-02: Drizzle generates type-safe queries", () => {
         it.todo("expectTypeOf(developers.$inferSelect).toMatchTypeOf<{ id: string; email: string }>()");
         it.todo("expectTypeOf(auditLog.$inferSelect.actorType).toEqualTypeOf<'developer' | 'user' | 'system'>()");
       });
       ```

    6. `packages/db/tests/seed.test.ts` — Plan 02 fills (will become an integration test against a Neon test branch). Per CONTEXT.md "Seed data" lock: ~5 developers + ~10 placeholder rows (mapped to users in Phase 1 — apps table doesn't exist yet):
       ```ts
       import { describe, it } from "vitest";
       describe("FOUND-02: seed populates expected counts", () => {
         it.todo("seeds at least 5 developers (CONTEXT lock: ~5 fake developers)");
         it.todo("seeds at least 10 users (CONTEXT lock: ~10 placeholder rows; apps table doesn't exist in Phase 1, so the count maps to users)");
       });
       ```

    7. `tooling/eslint-config/__tests__/no-raw-log.test.js` — Plan 03 fills (uses ESLint `RuleTester`):
       ```js
       const { describe, it } = require("vitest");
       describe("FOUND-04: no-raw-log rule", () => {
         it.todo("flags `logger.info(req.body)` as error");
         it.todo("flags `logger.error(err)` as error");
         it.todo("allows `logger.info({ event: 'name', ...safeFields })`");
         it.todo("allows `requestLogger.info({ ... })` (any identifier ending in `Logger`)");
       });
       ```

    **After all files written, run install + verify:**
    ```bash
    corepack enable
    corepack prepare pnpm@latest --activate   # ensures pnpm 10 is active
    pnpm install --no-frozen-lockfile
    ```
    (No frozen-lockfile on first install since pnpm-lock.yaml doesn't exist yet. Subsequent CI installs will use --frozen-lockfile.)

    Then run the full Phase 1 quick gate:
    ```bash
    pnpm typecheck && pnpm lint && pnpm test
    ```
    All three must exit 0. Biome may report formatting issues — run `pnpm lint:fix` to auto-format and re-run.

    Then verify the Next dev server boots (manual smoke per VALIDATION.md, but we can automate the boot check):
    ```bash
    pnpm --filter @indiepilot/web build
    ```
    Build must succeed. The dev-server smoke (`pnpm dev` shows "Ready in <Xs>") is left to the manual checkpoint in Plan 06.

    **Why stubs as `it.todo`:** Keeps `pnpm test` green from Plan 01 onward, so Wave 2 plans (02/03/04) can replace `it.todo` with real assertions one-at-a-time and immediately see RED→GREEN feedback. Avoids the bootstrapping problem where "test infra doesn't work yet so we can't verify anything."

    **Critical gotcha — pnpm-lock.yaml:** Commit `pnpm-lock.yaml` after the install. CI uses `--frozen-lockfile` (per Plan 05's `ci.yml`).
  </action>
  <verify>
    <automated>test -f apps/web/lib/__tests__/logger.test.ts && test -f apps/web/app/api/inngest/__tests__/route.test.ts && test -f apps/web/inngest/functions/__tests__/health.test.ts && test -f packages/db/tests/schema.test.ts && test -f packages/db/tests/types.test.ts && test -f packages/db/tests/seed.test.ts && test -f tooling/eslint-config/__tests__/no-raw-log.test.js && test -f pnpm-lock.yaml && pnpm typecheck && pnpm lint && pnpm test && pnpm --filter @indiepilot/web build</automated>
  </verify>
  <done>All seven Wave 0 stub test files exist with `it.todo` placeholders matching VALIDATION.md row-for-row. The seed stub asserts ≥5 developers + ≥10 users (the CONTEXT-locked "~10 placeholder rows" mapped to users since the apps table doesn't exist in Phase 1). `pnpm install` produced a `pnpm-lock.yaml`. `pnpm typecheck && pnpm lint && pnpm test && pnpm --filter @indiepilot/web build` all exit 0. The scaffold runs end-to-end. Plans 02/03/04 will replace `it.todo` with real assertions when they implement their respective features.</done>
</task>

</tasks>

<verification>
After all three tasks complete, the entire Phase 1 scaffold quick-gate runs in <30s:
```bash
pnpm typecheck && pnpm lint && pnpm test
```
And `pnpm --filter @indiepilot/web build` produces a working `.next/standalone/` output.

Manual smoke (deferred to Plan 06's checkpoint, but available now): `pnpm dev` should print `Ready in <Xs>` on `http://localhost:3000` and the placeholder page renders "IndiePilot — Phase 1 scaffold OK".

Wave 0 deliverables from VALIDATION.md (rows 1, 2, 3 — FOUND-01 typecheck/lint, smoke):
- `pnpm typecheck` automated check ✓
- `pnpm lint` automated check ✓
- `pnpm dev` smoke (manual — verified in Plan 06)
</verification>

<success_criteria>
- [ ] `pnpm-workspace.yaml` declares `apps/*`, `packages/*`, `tooling/*`
- [ ] Root `package.json` pins `pnpm@10` via `packageManager`
- [ ] Root `turbo.json` exists with the task graph from RESEARCH; `AXIOM_DATASET` appears exactly ONCE in the build env array (no duplicates)
- [ ] Biome 2.x + ESLint 9 sidecar both wired into `pnpm lint`
- [ ] `apps/web` is Next.js 16.2.4 + React 19; placeholder page renders at `/`
- [ ] `packages/ui` owns Tailwind v4 (`@import "tailwindcss"` + `@source` directives)
- [ ] `packages/db` is an empty shell with vitest config and tests/ folder
- [ ] All 7 Wave 0 stub test files exist with `it.todo` markers matching VALIDATION.md (seed stub uses 5 devs + 10 users per CONTEXT lock)
- [ ] `pnpm typecheck && pnpm lint && pnpm test` exits 0
- [ ] `pnpm --filter @indiepilot/web build` exits 0 (validates Next config + transpilePackages wiring)
- [ ] `pnpm-lock.yaml` is committed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Exact versions installed (Next 16.2.x, Drizzle deferred to Plan 02, etc.)
- Workspace structure as it landed on disk
- Any deviations from RESEARCH (e.g., if shadcn `init --monorepo` was used vs hand-written; if any version was bumped during install)
- Wave 0 stub test file locations + which plans will fill which `it.todo`
- **Notes/context-budget observation:** This plan touches ~30 files in 3 tasks. The executor should flag in the SUMMARY's Notes section any task that consumed >25% context — useful signal for splitting plans of similar scope in future phases. If all three tasks landed under target (~15-20% each), note that too as a positive data point.
</output>
