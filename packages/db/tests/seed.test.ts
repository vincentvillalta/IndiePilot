import { describe, it } from "vitest";

describe("FOUND-02: seed populates expected counts", () => {
  it.todo("seeds at least 5 developers (CONTEXT lock: ~5 fake developers)");
  it.todo(
    "seeds at least 10 users (CONTEXT lock: ~10 placeholder rows; apps table doesn't exist in Phase 1, so the count maps to users)",
  );
});
